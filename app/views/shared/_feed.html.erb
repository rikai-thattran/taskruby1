<% if @feed_items.any? %>
  <%= paginate @feed_items %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%# will_paginate @feed_items,
params: { controller: :static_pages, action: :home } %>
<% end %>
